{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [ {
            "label": "Build",
            "command": "g++ -o ${fileBasenameNoExtension} -g ${fileBasename}",
            "type": "shell",
            "args": [],
            "group": 
            {
                "kind": "build",
                "isDefault": true
            },
        },
        {
            "label": "Clean",
            "command": "rm -rf ${fileBasenameNoExtension}",
            "type": "shell",
            "args": []
        }
    ]
}